# Revisable assessment pilot — retained checkpoint, 9 September 2026

The bounded cross-tool transfer and fresh-AI continuation exercise ran. Both recipient
conditions scored **71/72** under the frozen rubric. No continuation advantage was
observed. The local view exists, but its first manual export failed because generated
JavaScript did not parse. That defect is repaired and the actual corrected script
passes syntax checking; a successful corrected browser workflow is still required.
**The full goal is not complete.**

## What the exercise establishes

Vela 0.977.6 accepted exact native Writ artifacts through its existing public CLI,
retained two pending Submissions and one scoped Verification Record, and replayed
those bytes in a separate checkout. Writ freshly checked two original mathematical
candidates and reconstructed two revision impacts from the received archive. The
reusable receiver rejects altered artifact bytes, an altered Verification signature,
and mismatched native mathematical subject identity. There are zero accepted Claims.
The disposable keys were removed; the receiver installs only its known synthetic
trust pin and removes that pin on completion. No third-party code was changed.

The CLI's accepted-target restriction rejected a correction to a pending Claim.
The supported route retains the original and later pending Submission, with explicit
native revision and mapping artifacts. It does not pretend that Vela's accepted-state
correction or Standing semantics transported Writ applicability. The earlier
[executed reuse report](REUSE_REPORT.md), [exact fixture bundle](evidence/synthetic-vela.bundle)
and [fresh receiving result](evidence/vela-receiving-final.json) preserve this boundary.
Vela's [pinned interoperability contract](https://github.com/vela-science/vela/blob/017bca6bfaa29e96d8e1b0819979c732d3320917/docs/INTEROPERABILITY.md)
and actual CLI/source inspection supplied the reusable implementation; no fork,
generic adapter registry, language migration or new mathematical machinery was needed.

The OPERA case keeps three analyst explanations, a disputed calibration assumption,
a conditional interpretation and two source updates. It preserves the initial report
when subsequent findings change its current use. Alpha's A1 premise and Beta's D1
objection are explicitly synthetic AI-authored analytic positions, not invented
human testimony. U1 withdraws A1 for current use; U2 corrects the anomaly interpretation.
The separate sizes and net contribution of the two timing effects remain unspecified.
There is no inferred prior, posterior, independence or causal effect, and no native
political-record promotion or mathematical certificate for the OPERA interpretation.

The evidence comes from three selected dated sections of
[CERN's evolving OPERA report](https://home.cern/opera-experiment-reports-anomaly-in-flight-time-of-neutrinos-from-cern-to-gran-sasso/).
The capture is a current publisher page, not three authenticated historical HTML
editions. The November 2011 and March 2012 sections and surrounding material are
outside this bounded packet. All selected reports share publisher provenance. The
portable packets retain short exact excerpts, byte hashes, source URL, dates, parent
capture hash and extraction spans. The original local capture's spans were checked
when constructing the packets. Only the small selected quotations are redistributed;
no blanket text licence or independent corroboration is assumed. Receiving verifies
excerpt bytes and declared references, not the publisher's truth or the experiment.

## Prospective comparison and actual observations

[Edition 1](packets/PROTOCOL.md), [task](packets/TASK.txt), paired packets and
[freeze manifest](packets/FREEZE.json) were committed at `bdeef24` before any recipient.
A separate receiver recovers the same substantive record values from both formats.
The prose baseline has explicit IDs, assumptions, dependencies, source metadata and
uncertainties. The only treatment is their representation as JSON fields or labelled
prose. Each receives the same incremental updates, with its own prior answers retained.
No future update or scoring feedback enters a recipient context.

Six actual fresh contexts completed three stages each. They used no parent-turn fork,
the same observed `gpt-6-astra` model label and `high` reasoning setting, the same tools,
one permitted packet read per stage and an 800-word limit. Exact model build, sampling
seed and temperature are unavailable. Recipient-chosen tool output caps differed,
but all files were returned in full without truncation. Every response is below the
shared word limit. There were no recipient retries, exclusions or other file reads.
Raw final responses were extracted directly from the execution transcript; none was
authored by the experiment coordinator. Private reasoning and full session logs are
not part of the retained evidence.

| Condition          | Run 1 | Run 2 | Run 3 | Total |
| ------------------ | ----: | ----: | ----: | ----: |
| Structured records | 24/24 | 24/24 | 23/24 | 71/72 |
| Competent prose    | 23/24 | 24/24 | 24/24 | 71/72 |

The condition-blinded scorer used eight prospectively specified dimensions per stage.
The root author inspected all responses and concurs with its two source-binding
deductions: structured run 3, stage 1 invents a preferred causal explanation for Beta;
prose run 1, stage 2 characterizes the later measurements as corrected measurements,
which is stronger than the supplied packet. The latter is a wording-sensitive judgment;
the scorer explicitly retained that ambiguity. Both preserve the actual original
history, withdrawal, conditional applicability and residual uncertainty under the
other rubric dimensions. [Raw scorer output](evidence/blinded-scorer.json),
[run identities and observations](evidence/pilot-observations.json), all
[recipient outputs](evidence/recipients/), and arithmetic/identity receiving remain
available. The receiver does not prove the semantic merit of the scoring judgments.

This is a familiar historical example with extensive explanatory scaffolding. Same-model
recipients and scorer, three runs per condition, possible prior historical knowledge,
behavioral rather than OS-enforced isolation, and a near-ceiling rubric constrain the
finding. Do not infer equality in other tasks, general superiority, efficiency, human
benefit or real-world analytic validity. The frozen human follow-up instructions use
the same packets and rubric; no human comparison study or recruitment took place.

## Local interface and assurance

The [local notebook package](../../../packages/assessment-view/README.md) uses existing
Writ provenance and shared-analysis libraries. It exposes dated sources, hypotheses,
A1/D1/C2 and their declared dependencies, unresolved questions, and the separately
labelled native synthetic archive. Static summaries are bound to this frozen packet
edition. An author's continuation appends an explicit A1 position and rationale;
it never edits the source history or automatically resolves physical truth.

Its receiver independently reconstructs the permitted append operation with the
producer disabled, sharing the established validation/hash primitives. It rejects
stale base identity, unknown sources or operations, altered source excerpts, changed
history, mismatched requests and Unicode substitutions that canonical normalization
alone would equate. Exact source text remains separate from canonical object identity.
The synthetic CLI request and receipt are retained; they are not a user browser result.

The first manual attempt selected Withdrawn and a rationale but produced no visible
download. The retained [failure record](evidence/browser-attempt-1.json) binds the original
artifact. A literal newline in a generated single-quoted string prevented the whole
client script from parsing. `String.raw` fixes the nesting; a regression compiles the
actual rendered script. A visible generated-JSON field and select/save choices now
provide a fallback, and the page explicitly says it cannot confirm a saved file.
This is a code-level repair, not a successful browser observation.

Browser Use rejected the local file URL and explicitly prohibited alternate routes,
raw CDP, indirect execution or other workarounds. No bypass was attempted. The corrected
HTML is available locally for one manual workflow, followed by independent receiving of
the resulting request. Desktop/mobile rendering, keyboard interaction, successful save
or fallback use and the full corrected browser round trip remain unverified. Unit
checks and the mechanical design detector do not substitute for those observations.

A later user-reported copy of generated JSON passes structure, base identity and
producer-disabled append receiving. Its values remain exactly `contested` and the
rationale `continuation-request.json`. The [copy context](evidence/user-reported-context.json)
explicitly distinguishes locally serialized semantic values from authenticated original
download bytes. The filename-like rationale supplies no substantive scientific reason;
it is not corrected silently or treated as endorsement. This is partial browser-path
evidence, not confirmation of saved bytes, the intended Withdrawn transition, navigation
or the full corrected workflow.

## Validation and next gate

At `6200eb1`, required format, lint, typecheck, test, data check, Writ verification and
build passed. Real native integration passed 16 Decision Case, 2 Shared Analysis and
12 Certificate Transport tests with no skips. The Python suite passed 75 tests;
pack validation, source-registry checking, Ruff and mypy passed. A parallel provenance
rebuild initially caused one integration import failure; its sequential retry passed.
The local Python environment did not activate its editable source path even after
installation; explicit `PYTHONPATH=apps/ingest/src` resolved collection and registry
checking. These setup failures remain recorded and are not rewritten as initial passes.
The corrected view/receiver now has eight focused tests; final source-bound validation
will distinguish that later source from the first checkpoint.

No new theorem, formal verification or empirical physics result is claimed. The work
advances bounded cross-tool provenance, revisability and handoff evidence. It supplies
no new strategic disclosure, strategic history-abstraction or mechanism-composition
result. The strategic-security moonshot remains the central long-term destination;
this nonsecurity example tests an enabling workflow only. Decision Lab, Bellman,
third-party source and the unmerged decision-episode branch remain untouched.

The next gate is a successful corrected local browser continuation and independent
receiving, followed by its additive evidence and final acceptance audit. Do not widen
this pilot into a graph platform, another inference model or a new empirical campaign
while that concrete workflow remains unverified.
