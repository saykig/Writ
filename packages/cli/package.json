{
  "name": "@covenant/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "lint": "eslint .",
    "format": "prettier --check ."
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "covenant": "./bin/covenant.ts"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@covenant/analyzer": "workspace:*",
    "@covenant/domain": "workspace:*",
    "@covenant/evaluator": "workspace:*",
    "@covenant/language": "workspace:*"
  }
}
