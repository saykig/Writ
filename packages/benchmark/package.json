{
  "name": "@writ/benchmark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "lint": "eslint .",
    "format": "prettier --check .",
    "run-benchmark": "bun run src/run.ts"
  },
  "dependencies": {
    "@writ/domain": "workspace:*",
    "@writ/evaluator": "workspace:*",
    "@writ/analyzer": "workspace:*",
    "@writ/provenance": "workspace:*",
    "@writ/language": "workspace:*"
  }
}
