# Verification fixtures

- `compatibility/` preserves inputs required to verify historical behavior.
- `language/` contains compiler, parser, and analyzer fixtures.
- `schemas/` contains schema-validation fixtures.

These are active test inputs, not examples of Writ's current product or corpus model.
