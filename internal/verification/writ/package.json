{
  "name": "@writ/verification-harness",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test --timeout 60000",
    "lint": "eslint .",
    "format": "prettier --check ."
  },
  "dependencies": {
    "@writ/domain": "workspace:*",
    "@writ/language": "workspace:*",
    "@writ/provenance": "workspace:*",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
