# Verification

This tree contains active tests and frozen inputs used to prove Writ behavior. Benchmarks test
derived behavior, conformance cases define implementation-independent expectations, and fixtures
support package or integration tests. None of these paths is a political corpus authority.

`writ/` contains the read-only, authority-traced Writ Verification Harness documented in
`docs/verification/verification-harness.md`.
