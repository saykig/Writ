# Build + tooling artifacts
node_modules/
dist/
coverage/
bun.lock
bun.lockb
*.langium
.next/
**/.next/
next-env.d.ts
**/components/ui/

# Authored specification + evidence content — kept byte-stable (golden JSON,
# canonical IR, receipts, and governance docs must never be reformatted).
packages/domain/schemas/
internal/verification/fixtures/
internal/infrastructure/generated/
docs/
adr/
codex-tasks/
.agents/
internal/infrastructure/database/
internal/tooling/scripts/
MANIFEST.sha256
AGENTS.md
TASKS.yaml
VALIDATION.md
VERSION_POLICY.md

# Generated canonical corpora, emitted by the Python adapters via
# canonical_json_bytes. Their emitters are asserted to be byte-for-byte
# reproducible, so reformatting them here would break those tests.
benchmark/*/normalized/
pilot/*/normalized/
archive/pilots/eu-us-ai-evaluation-v1/

# Generated embed outputs (regenerated on build)
**/schemas.embedded.ts

# Generated by historical pilot scripts.
# Written canonically so their hashes reproduce; reformatting would only drift
# them from what the builders emit.
corpora/jurisdictions/

# Byte-preserved official source captures may contain publisher HTML that is not
# Prettier-parseable. Their source manifests and evidence hashes guard the bytes.
corpora/institutional/**/sources/captures/

# The reviewed EU/US legal-policy corpora are emitted byte-for-byte by
# writ_ingest.corpus.eu_us_ai_governance.canonical_yaml_bytes and checked with
# `--check`. Reformatting them here would drift them from their generator.
# Hand-authored corpora (constitutional law, institutional) are not listed.
corpora/legal-policy/eu/
corpora/legal-policy/us/nist/
corpora/legal-policy/us/office-of-management-and-budget/
corpora/legal-policy/us/white-house/

# Frozen archived compatibility datasets; preserved byte-for-byte.
archive/compatibility/
