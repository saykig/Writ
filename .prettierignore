# Build + tooling artifacts
node_modules/
dist/
coverage/
bun.lock
*.langium

# Authored specification + evidence content — kept byte-stable (golden JSON,
# canonical IR, receipts, and governance docs must never be reformatted).
packages/domain/schemas/
internal/verification/fixtures/
internal/infrastructure/generated/
docs/
adr/
.agents/
internal/tooling/scripts/
MANIFEST.sha256
AGENTS.md
TASKS.yaml

# Portable decision-case subjects and exact engine artifacts retain raw byte identities.
examples/decision-cases/**/case.json
examples/decision-cases/**/*.case.json
examples/decision-cases/**/executions/*.json

# Generated embed outputs (regenerated on build)
**/schemas.embedded.ts

# The reviewed EU/US legal-policy corpora are emitted byte-for-byte by
# writ_ingest.corpus.eu_us_ai_governance.canonical_yaml_bytes and checked with
# `--check`. Reformatting them here would drift them from their generator.
# Hand-authored corpora (constitutional law, institutional) are not listed.
corpora/legal-policy/eu/
corpora/legal-policy/us/nist/
corpora/legal-policy/us/office-of-management-and-budget/
corpora/legal-policy/us/white-house/
