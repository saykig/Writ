# Historical benchmark artifacts

Benchmark inputs, generated DSL predictions, hidden official labels, and discrepancy records must
remain separate. A prediction is generated and hashed before an official historical label is loaded
for comparison. A disagreement requires review and is never silently corrected.

Phase 1A produces no G20 benchmark sample.
