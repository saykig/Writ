# Track B role outcomes

| Task / commits | Role | Unique correctness issue discovered | False positive or duplicated work | Reusable lesson promoted | Evidence or permanent protection |
| --- | --- | --- | --- | --- | --- |
| `TRACK-B-CROSS-LAYER-CONSISTENCY-001`; `08a353d` | Contract / Schema Reviewer | Exact-version validation silently fell back to the current validator for unsupported versions. | Record-link representation disagreement duplicated the export reviewer's root finding. A duplicate-singular-member anomaly was not independently classified before the stop condition. | Exact requested versions must resolve through the registry before validation. | Unsupported-version record and judgment regressions in `packages/domain/test/`; `DOMAIN_SCHEMA_VERSION_UNSUPPORTED`. |
| `TRACK-B-CROSS-LAYER-CONSISTENCY-001`; `08a353d`, `d1f2f60` | Provenance Consistency Reviewer | A false embedded source SHA and routed source-version drift survived portable reload; independently confirmed exact-version fallback and record/link replay findings. | Contract fallback duplicated the contract reviewer. A cross-corpus historical-target anomaly was left unclassified when the repair limit was reached. | Exact source envelopes must bind their own bytes before derived evidence is trusted. | Source-hash/source-version regressions in `packages/data-bundle/test/review-artifact.test.ts`; all NIST provenance gates and bound-successor controls remain green. |
| `TRACK-B-CROSS-LAYER-CONSISTENCY-001`; `d1f2f60` | Export / Interoperability Reviewer | Portable reload accepted disagreement among record/link projections, native values, retained fragments, routed resources and nested evidence. | Several status and evidence mutations were duplicate expressions of one missing replay boundary; unreferenced-resource completeness was correctly separated from internal agreement. | Reuse the production projector against bundle-contained resources and compare complete reconstructed object sets. | `assertNativeRecordAndLinkIntegrity`; exact record/link/source/catalog/corpus regressions; bundle `1.0.0` and `1.1.0` controls. |
| `TRACK-B-CROSS-LAYER-CONSISTENCY-001`; `d1f2f60` | Independent Counterexample Reviewer | Coherently duplicating a corpus plus matching record/link arrays bypassed per-corpus replay and global key uniqueness. | Independently reproduced the main record/link divergence; no proposed blocker disappeared under control. | A per-object equivalence proof also needs complete-set identity uniqueness and an exact-equal control. | Shared `assertUniqueCanonicalObjectKeys`; coherent duplicate-corpus regression; disposable production-entry-point harness. |
| `TRACK-B-HISTORICAL-JUDGMENT-TARGET-SCOPE-001`; `ea93a18` | Provenance Consistency Reviewer | A global migration-ID exception let another corpus's record migration excuse an absent superseded record target and let record history excuse an absent record-link target. | The same-corpus superseded record control was legitimate; accepted judgments and unrelated missing IDs already failed closed. | An exception for historical identity must retain the namespace and object kind of the migration authority that justifies it. | Corpus-scoped historical record-ID index in `verifyProvenance`; same-corpus, cross-corpus and wrong-kind regressions in `verifier.test.ts`. |
| `TRACK-B-HISTORICAL-JUDGMENT-TARGET-SCOPE-001`; `ea93a18` | Independent Counterexample Reviewer | Independently reproduced both scope failures using the opposite NIST-to-Commission corpus direction and both provenance verification entry points. | Confirmed the same root defect rather than proposing a second repair; the untouched repository remained green. | Mirror a namespace differential in the opposite direction and retain current/unrelated controls before narrowing a historical exception. | Read-only NIST-based snapshot fixtures confirmed `HISTORICAL_TARGET_CORPUS_AND_KIND_DEFECT`; permanent regressions cover the shared invariant. |
