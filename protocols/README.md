# Protocol authority

Protocol contracts are separate from JSON Schema authority:

- `language/writ.ebnf` defines the language grammar protocol.
- `api/openapi.yaml` defines the API protocol.

JSON Schema authority lives only under `schemas/`.
